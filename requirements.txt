name: Test SerpAPI

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests
      - name: Run SerpAPI test
        env:
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
        run: python test_serpapi.py
